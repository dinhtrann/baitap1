# 2.5.3 Xem & Thanh Toán Khoản Phạt (Nhân Viên Thư Viện) - Librarian Confirm Penalty Payment Flow

## Mô tả
Tính năng cho phép nhân viên thư viện xem và xác nhận/từ chối thanh toán các khoản phạt từ độc giả.

**Actor:** Nhân viên thư viện  
**Mã số:** 2.5.3  
**Yêu cầu:** Đăng nhập với vai trò nhân viên thư viện

## Flowchart

```mermaid
flowchart TD
    Start([Nhân viên truy cập trang<br/>Quản lý khoản phạt]) --> CheckAuth{Kiểm tra đăng nhập}
    
    CheckAuth -->|Chưa đăng nhập| RedirectLogin[Chuyển hướng đến<br/>trang đăng nhập]
    RedirectLogin --> End([Kết thúc])
    
    CheckAuth -->|Đã đăng nhập| CheckRole{Kiểm tra vai trò}
    
    CheckRole -->|Không phải Librarian| ErrorUnauthorized[Hiển thị lỗi:<br/>Không có quyền truy cập]
    ErrorUnauthorized --> End
    
    CheckRole -->|Là Librarian| LoadPenalties[Tải danh sách khoản phạt<br/>chưa thanh toán]
    LoadPenalties --> DisplayList[Hiển thị danh sách:<br/>- Độc giả<br/>- Nguyên nhân phạt<br/>- Số tiền<br/>- Ngày phạt<br/>- Trạng thái<br/>- Nút Xem chi tiết]
    DisplayList --> UserClick{User click<br/>Xem chi tiết?}
    
    UserClick -->|Không| End
    
    UserClick -->|Có| ShowDetail[Hiển thị chi tiết phiếu phạt:<br/>- Độc giả<br/>- Nguyên nhân phạt<br/>- Số tiền<br/>- Ngày phạt<br/>- Trạng thái<br/>- Thông tin chuyển khoản<br/>- Nút Đã thanh toán<br/>- Nút Từ chối]
    ShowDetail --> CheckStatus{Kiểm tra trạng thái}
    
    CheckStatus -->|Không phải Chờ xác nhận| ShowReadOnly[Chỉ hiển thị thông tin<br/>Không có nút hành động]
    ShowReadOnly --> End
    
    CheckStatus -->|Chờ xác nhận| UserAction{User chọn hành động}
    
    UserAction -->|Click Đã thanh toán| ConfirmFlow[Flow Xác nhận]
    UserAction -->|Click Từ chối| RejectFlow[Flow Từ chối]
    
    ConfirmFlow --> VerifyPayment{Kiểm tra thông tin<br/>chuyển khoản}
    VerifyPayment -->|Số tiền không khớp| ErrorAmount[Hiển thị cảnh báo:<br/>Số tiền không khớp<br/>Bạn có chắc muốn xác nhận?]
    ErrorAmount --> UserConfirm{User vẫn xác nhận?}
    
    UserConfirm -->|Hủy| ShowDetail
    UserConfirm -->|Xác nhận| UpdateStatusConfirm[Cập nhật trạng thái:<br/>Chờ xác nhận → Đã thanh toán]
    
    VerifyPayment -->|Số tiền khớp| UpdateStatusConfirm
    
    UpdateStatusConfirm --> SaveDB[Lưu vào database]
    SaveDB --> CheckSave{Kiểm tra kết quả}
    
    CheckSave -->|Lỗi| ErrorDB[Hiển thị lỗi:<br/>Lỗi hệ thống]
    ErrorDB --> ShowDetail
    
    CheckSave -->|Thành công| SuccessConfirm[Hiển thị thông báo:<br/>Xác nhận thanh toán thành công]
    SuccessConfirm --> Refresh[Làm mới danh sách]
    Refresh --> End
    
    RejectFlow --> ShowRejectForm[Hiển thị form từ chối:<br/>Nhập lý do từ chối]
    ShowRejectForm --> InputReason[User nhập lý do từ chối]
    InputReason --> ValidateReason{Kiểm tra lý do}
    
    ValidateReason -->|Lý do để trống| ErrorEmptyReason[Hiển thị lỗi:<br/>Lý do từ chối không được để trống]
    ErrorEmptyReason --> InputReason
    
    ValidateReason -->|Lý do hợp lệ| UpdateStatusReject[Cập nhật trạng thái:<br/>Chờ xác nhận → Từ chối]
    UpdateStatusReject --> SaveReason[Lưu lý do từ chối]
    SaveReason --> SaveDB2[Lưu vào database]
    SaveDB2 --> CheckSave2{Kiểm tra kết quả}
    
    CheckSave2 -->|Lỗi| ErrorDB2[Hiển thị lỗi:<br/>Lỗi hệ thống]
    ErrorDB2 --> InputReason
    
    CheckSave2 -->|Thành công| SuccessReject[Hiển thị thông báo:<br/>Đã từ chối thanh toán]
    SuccessReject --> Refresh2[Làm mới danh sách]
    Refresh2 --> End
    
    InputReason -->|Hủy| Cancel[Đóng form]
    Cancel --> ShowDetail
```

## Trạng Thái Phiếu Phạt

### Khi Xác Nhận
- Trạng thái: **"Chờ xác nhận"** → **"Đã thanh toán"**
- Nhân viên kiểm tra thông tin chuyển khoản và số tiền

### Khi Từ Chối
- Trạng thái: **"Chờ xác nhận"** → **"Từ chối"**
- Lưu lý do từ chối
- Phiếu phạt vẫn ở trạng thái chưa thanh toán

## Validation Rules

- **Lý do từ chối:** Bắt buộc phải nhập (không được để trống)

## Kiểm Tra Thanh Toán

- Nhân viên kiểm tra:
  - Số tiền chuyển khoản có khớp với số tiền phạt không
  - Nội dung chuyển khoản có đúng không
  - Thời gian chuyển khoản

## Error Cases

- Chưa đăng nhập hoặc không phải Librarian
- Phiếu phạt không tồn tại
- Phiếu phạt không ở trạng thái "Chờ xác nhận"
- Lý do từ chối để trống (khi từ chối)
- Lỗi cập nhật database

