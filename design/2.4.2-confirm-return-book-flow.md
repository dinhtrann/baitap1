# 2.4.2 Xác Nhận Trả Sách - Confirm Return Book Flow

## Mô tả
Tính năng cho phép nhân viên thư viện xác nhận trả sách và xử lý các trường hợp sách bình thường, hư hỏng, hoặc mất.

**Actor:** Nhân viên thư viện  
**Mã số:** 2.4.2  
**Yêu cầu:** Đăng nhập với vai trò nhân viên thư viện

## Flowchart

```mermaid
flowchart TD
    Start([Nhân viên vào trang<br/>Quản lý mượn trả<br/>Tab Chờ xác nhận trả]) --> CheckAuth{Kiểm tra đăng nhập}
    
    CheckAuth -->|Chưa đăng nhập| RedirectLogin[Chuyển hướng đến<br/>trang đăng nhập]
    RedirectLogin --> End([Kết thúc])
    
    CheckAuth -->|Đã đăng nhập| CheckRole{Kiểm tra vai trò}
    
    CheckRole -->|Không phải Librarian| ErrorUnauthorized[Hiển thị lỗi:<br/>Không có quyền truy cập]
    ErrorUnauthorized --> End
    
    CheckRole -->|Là Librarian| LoadReturnRequests[Tải danh sách yêu cầu trả<br/>trạng thái: Chờ xác nhận]
    LoadReturnRequests --> DisplayList[Hiển thị danh sách:<br/>- Tên sách<br/>- Độc giả<br/>- Ngày mượn<br/>- Ngày hết hạn<br/>- Nút Xác nhận trả]
    DisplayList --> UserClick{User click<br/>Xác nhận trả?}
    
    UserClick -->|Không| End
    
    UserClick -->|Có| ReceiveBook[Nhận sách vật lý<br/>từ độc giả]
    ReceiveBook --> ShowForm[Hiển thị form xác nhận:<br/>Chọn tình trạng sách]
    ShowForm --> SelectCondition{Chọn tình trạng sách}
    
    SelectCondition -->|Bình thường| NormalFlow[Flow Bình thường]
    SelectCondition -->|Hư hỏng| DamagedFlow[Flow Hư hỏng]
    SelectCondition -->|Mất| LostFlow[Flow Mất]
    
    NormalFlow --> ValidateNormal{Kiểm tra validation}
    ValidateNormal -->|Tình trạng chưa chọn| ErrorCondition[Hiển thị lỗi:<br/>Vui lòng chọn tình trạng sách]
    ErrorCondition --> ShowForm
    
    ValidateNormal -->|Tất cả hợp lệ| UpdateStatusNormal[Cập nhật đơn mượn:<br/>Đã trả]
    UpdateStatusNormal --> IncreaseAvailable[Tăng số lượng sách có sẵn]
    IncreaseAvailable --> DecreaseBorrowed[Giảm số lượng sách đang mượn]
    DecreaseBorrowed --> SaveDBNormal[Lưu vào database]
    SaveDBNormal --> SuccessNormal[Hiển thị thông báo:<br/>Xác nhận trả sách thành công]
    SuccessNormal --> Refresh[Làm mới danh sách]
    Refresh --> End
    
    DamagedFlow --> SelectPenalty[Chọn mức phạt<br/>từ danh sách]
    SelectPenalty --> InputNote[Nhập ghi chú<br/>tối đa 500 ký tự]
    InputNote --> CheckLate{Kiểm tra trả muộn?}
    
    CheckLate -->|Trả muộn| CreateLatePenalty[Tạo phiếu phạt<br/>Trả muộn]
    CheckLate -->|Không muộn| CreateDamagePenalty[Tạo phiếu phạt<br/>Hư hỏng]
    
    CreateLatePenalty --> CreateDamagePenalty
    CreateDamagePenalty --> ValidateDamaged{Kiểm tra validation}
    
    ValidateDamaged -->|Mức phạt chưa chọn| ErrorPenalty[Hiển thị lỗi:<br/>Vui lòng chọn mức phạt]
    ErrorPenalty --> SelectPenalty
    
    ValidateDamaged -->|Ghi chú để trống hoặc >500 ký tự| ErrorNote[Hiển thị lỗi:<br/>Ghi chú không hợp lệ]
    ErrorNote --> InputNote
    
    ValidateDamaged -->|Tất cả hợp lệ| UpdateStatusDamaged[Cập nhật đơn mượn:<br/>Đã trả]
    UpdateStatusDamaged --> MarkDamaged[Đánh dấu sách hư hỏng]
    MarkDamaged --> SaveDBDamaged[Lưu vào database]
    SaveDBDamaged --> SuccessDamaged[Hiển thị thông báo:<br/>Xác nhận trả sách thành công<br/>Đã tạo phiếu phạt]
    SuccessDamaged --> Refresh
    Refresh --> End
    
    LostFlow --> SelectPenaltyLost[Chọn mức phạt<br/>từ danh sách]
    SelectPenaltyLost --> InputNoteLost[Nhập ghi chú<br/>tối đa 500 ký tự]
    InputNoteLost --> ValidateLost{Kiểm tra validation}
    
    ValidateLost -->|Mức phạt chưa chọn| ErrorPenaltyLost[Hiển thị lỗi:<br/>Vui lòng chọn mức phạt]
    ErrorPenaltyLost --> SelectPenaltyLost
    
    ValidateLost -->|Ghi chú để trống hoặc >500 ký tự| ErrorNoteLost[Hiển thị lỗi:<br/>Ghi chú không hợp lệ]
    ErrorNoteLost --> InputNoteLost
    
    ValidateLost -->|Tất cả hợp lệ| UpdateStatusLost[Cập nhật đơn mượn:<br/>Đã trả]
    UpdateStatusLost --> MarkLost[Đánh dấu sách mất]
    MarkLost --> CreateLostPenalty[Tạo phiếu phạt Mất]
    CreateLostPenalty --> SaveDBLost[Lưu vào database]
    SaveDBLost --> SuccessLost[Hiển thị thông báo:<br/>Xác nhận trả sách thành công<br/>Đã tạo phiếu phạt]
    SuccessLost --> Refresh
    Refresh --> End
```

## Các Trường Hợp Xử Lý

### 1. Bình Thường
- Chọn tình trạng: **"Bình thường"**
- Cập nhật đơn mượn thành **"Đã trả"**
- Tăng số lượng sách có sẵn
- Giảm số lượng sách đang mượn
- Không tạo phiếu phạt

### 2. Hư Hỏng
- Chọn tình trạng: **"Hư hỏng"**
- **Bắt buộc:** Chọn mức phạt và nhập ghi chú
- Kiểm tra trả muộn:
  - Nếu muộn: Tạo **2 phiếu phạt** (Trả muộn + Hư hỏng)
  - Nếu không muộn: Tạo **1 phiếu phạt** (Hư hỏng)
- Cập nhật đơn mượn thành **"Đã trả"**
- Đánh dấu sách hư hỏng

### 3. Mất
- Chọn tình trạng: **"Mất"**
- **Bắt buộc:** Chọn mức phạt và nhập ghi chú
- Tạo phiếu phạt **"Mất"**
- Cập nhật đơn mượn thành **"Đã trả"**
- Đánh dấu sách mất

## Validation Rules

- **Tình trạng sách:** Bắt buộc chọn (bình thường, hư hỏng, mất)
- **Mức phạt:** Bắt buộc khi tình trạng là "hư hỏng", trả muộn hoặc "mất"
- **Ghi chú:** Bắt buộc khi tình trạng là "hư hỏng" hoặc "mất", tối đa 500 ký tự

## Chọn Mức Phạt

- Khi sách hư hỏng hoặc mất, nhân viên phải chọn một **mức phạt** từ danh sách các mức phạt đã được quản lý viên cấu hình (flow 2.5.1)

## Error Cases

- Chưa đăng nhập hoặc không phải Librarian
- Yêu cầu trả sách không tồn tại
- Yêu cầu không ở trạng thái "Chờ xác nhận"
- Tình trạng sách chưa được chọn
- Mức phạt chưa được chọn (khi hư hỏng hoặc mất)
- Ghi chú để trống hoặc quá dài (>500 ký tự) (khi hư hỏng hoặc mất)
- Mức phạt không tồn tại
- Lỗi tạo phiếu phạt
- Lỗi cập nhật database

